import { HistoricalReport } from './historical-report.model';

describe('HistoricalReport', () => {
  it('should create an instance', () => {
    expect(new HistoricalReport()).toBeTruthy();
  });
});
