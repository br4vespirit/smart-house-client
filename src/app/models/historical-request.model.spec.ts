import { HistoricalRequest } from './historical-request.model';

describe('HistoricalRequest', () => {
  it('should create an instance', () => {
    expect(new HistoricalRequest()).toBeTruthy();
  });
});
